{
  "name": "inventory",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domain/inventory/src",
  "projectType": "library",
  "tags": [
    "type:domain",
    "scope:inventory"
  ],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/domain/inventory"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/domain/inventory"
      }
    }
  }
}
