{
  "name": "db-postgres",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/data/db-postgres/src",
  "projectType": "library",
  "tags": [
    "type:lib",
    "scope:data"
  ],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/data/db-postgres"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/data/db-postgres"
      }
    }
  }
}
