{
  "name": "payment-orchestrator",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/payments/payment-orchestrator/src",
  "projectType": "library",
  "tags": [
    "type:lib",
    "scope:payments"
  ],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/payments/payment-orchestrator"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/payments/payment-orchestrator"
      }
    }
  }
}
