{
  "name": "sync-engine",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/sync/sync-engine/src",
  "projectType": "library",
  "tags": [
    "type:lib",
    "scope:sync"
  ],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/sync/sync-engine"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/sync/sync-engine"
      }
    }
  }
}
