{
  "name": "analytics-contracts",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared/analytics-contracts/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/shared/analytics-contracts"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run libs/shared/analytics-contracts" }
    }
  }
}

