{
  "name": "erp-platform",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/erp/erp-platform/src",
  "tags": ["type:lib", "scope:erp", "layer:runtime"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/erp/erp-platform"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/erp/erp-platform"
      }
    }
  }
}
