{
  "name": "app-registry",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/platform/app-registry/src",
  "projectType": "library",
  "tags": [
    "type:lib",
    "scope:platform"
  ],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/platform/app-registry"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run libs/platform/app-registry"
      }
    }
  }
}
