// Availability rules are shared with the transactional app — one definition of
// what "for sale here" means, so the two hosts cannot tell a shopper different
// things about the same product.
export { availabilityState, type AvailabilityState } from '@retail-os/ui-react';
