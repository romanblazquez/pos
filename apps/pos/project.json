{
  "name": "pos",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/pos/src",
  "projectType": "application",
  "tags": ["type:app", "scope:pos"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "vite --config apps/pos/vite.config.mts" }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "vite build --config apps/pos/vite.config.mts" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p tsconfig.json", "cwd": "apps/pos" }
    }
  }
}
